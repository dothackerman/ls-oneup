# v0.0.1 - 2026-03-03

## Summary
Initial prototype release for `ls-oneup`.

## Included
1. Milestone 1 (Probe Context Capture MVP) baseline implementation.
2. Local-first quality tooling (`format`, `lint`, `typecheck`, integration and E2E test scripts).
3. Cloudflare Workers + D1 + R2 deployment scaffolding and runbook.

## Notes
1. This is the first tagged project release.
2. Cloudflare deployment remains a manual operator step per repo policy.
